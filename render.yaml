services:
  - type: web
    name: quiz-solver
    env: python
    buildCommand: "./render-build.sh"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: AIPIPE_API_KEY
        sync: false
      - key: EXPECTED_SECRET
        sync: false
